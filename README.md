# MyChain DEX (Simple XYK AMM)

A minimal AMM for your private L1 to set an initial price for MYC against USDx.

## Setup
```bash
git clone <this repo> && cd mychain-dex
cp .env.example .env
# edit RPC_URL / PRIVATE_KEY / CHAIN_ID
npm i
