{
  "name": "drl_digital_coin",
  "version": "1.0.0",
  "private": true,
  "description": "DRL Coin on your L1 + deploy script",
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat run scripts/00_deploy.js --network mychain"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.26.2"
  }
}
